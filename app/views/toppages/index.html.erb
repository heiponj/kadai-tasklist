<% if logged_in? %>
  <p>ログイン完了ユーザ: <%= current_user.name %></p>
<% else %>
  <div>
      <h1>タスク管理アプリ</h1>
      <%= link_to "新規登録", "/signup" %>
      <%= link_to "ログイン", "/login" %>
  </div>
<% end %>